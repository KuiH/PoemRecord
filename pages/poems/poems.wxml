
<view class="container">
  <view>诗人：{{poetName}}</view>
  <view class="poems-list">
    <view class="poem" wx:for="{{poems}}" wx:for-item="poem" wx:key="index">
      <view>{{poem.title}}</view>
      <view>{{poem.content}}</view>
    </view>
  </view>
</view>